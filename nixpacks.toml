[phases.setup]
nixPkgs = ["python39", "postgresql"]
aptPkgs = ["libpq-dev"]

[start]
cmd = "gunicorn sistema_gonnet.wsgi --preload --bind 0.0.0.0:$PORT --workers ${WEB_CONCURRENCY:-1} --threads=4 --timeout=120 --max-requests=500 --max-requests-jitter=50 --log-file -"
